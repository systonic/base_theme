(function ($, Drupal, once) {

  Drupal.behaviors.base_theme = {
    attach: function (context, settings) {

      //once('label', '#selector').forEach(function (item) {});

    }
  };

})(jQuery, Drupal, once);
